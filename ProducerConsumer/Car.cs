namespace ProducerConsumer;

public class Car
{
    public string Model { get; set; }
    
    // Konstruktor, setzt das Model des Autos
    public Car(string model)
    {
        Model = model;
    }
}